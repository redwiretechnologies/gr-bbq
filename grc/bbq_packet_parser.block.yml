id: bbq_packet_parser
label: Redi Chek Parser
category: '[BBQ]'

templates:
  imports: from gnuradio import bbq
  make: bbq.packet_parser()

inputs:
- label: bits_in
  domain: message

outputs:
- label: data_out
  domain: message

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
